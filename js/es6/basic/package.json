{
  "name": "customer-list",
  "version": "1.0.0",
  "description": "Display customers detail in a table with search function",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "hello": "nodemon  index.js",
    "compile": "babel src --watch  --compilers js:babel-core/register  --out-file dist/app.js",
    "bundle": "watchify dist/app.js  --ignore-missing=true -o dist/bundle.js",
    "dev": "concurrently --kill-others \"npm run compile\" \"npm run bundle\" "
  },
  "keywords": [
    "es6",
    "babel"
  ],
  "author": "harryho",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "browserify": "^16.1.0",
    "express": "^4.16.2",
    "watchify": "^3.11.0"
  },
  "dependencies": {
    "babel-plugin-transform-regenerator": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-polyfill": "^6.26.0",
    "concurrently": "^3.5.1",
    "nodemon": "^1.17.1"
  }
}
